# Docker Baseline

Task 1 provides a safe local PostgreSQL service through the root `docker-compose.yml`.

Application containers are not defined yet because the backend and frontend runtime contracts are still skeleton-only.
