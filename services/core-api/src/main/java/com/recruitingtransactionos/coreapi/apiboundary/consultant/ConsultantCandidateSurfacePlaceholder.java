package com.recruitingtransactionos.coreapi.apiboundary.consultant;

/**
 * Placeholder retained intentionally so the abandoned candidate CRUD experiment remains visible in
 * the tree without participating in Spring controller scanning or API contract tests.
 */
final class ConsultantCandidateSurfacePlaceholder {

  private ConsultantCandidateSurfacePlaceholder() {
  }
}
